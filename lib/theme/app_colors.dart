import 'package:flutter/material.dart';

class AppColors {
  /// COMMON
  static const primary = Color(0xFF2979FF);
  static const positive = Color(0xFF00C853);
  static const negative = Color(0xFFD50000);

  /// DARK
  static const darkBackground = Color(0xFF0F1117);
  static const darkSurface = Color(0xFF171A23);
  static const darkCard = Color(0xFF1F2330);
  static const darkText = Colors.white;
  static const darkTextSecondary = Colors.white70;

  /// LIGHT
  static const lightBackground = Color(0xFFF5F7FA);
  static const lightSurface = Colors.white;
  static const lightCard = Color(0xFFE9EEF3);
  static const lightText = Color(0xFF1A1A1A);
  static const lightTextSecondary = Color(0xFF555555);
}
